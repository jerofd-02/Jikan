FROM node:lts-alpine

WORKDIR /app

COPY package*.json ./
RUN npm ci --omit=dev && npm install -g nodemon --ignore-scripts

COPY . .

EXPOSE 3000
EXPOSE 9229

CMD ["nodemon", "--inspect=0.0.0.0:9229",  "app.js"]