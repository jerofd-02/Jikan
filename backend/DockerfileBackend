FROM node:lts-alpine

WORKDIR /app

RUN apk add --no-cache python3 make g++

COPY package*.json ./
RUN npm install --omit=dev && npm install -g nodemon --ignore-scripts

COPY . .

EXPOSE 3000
EXPOSE 9229

CMD ["nodemon", "--inspect=0.0.0.0:9229",  "app.js"]